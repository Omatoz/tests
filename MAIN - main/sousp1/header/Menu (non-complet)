#include <stdio.h>
#include "header.h"

void menu() {
    int choix =0;
    int choix2 = 0;
    printf("Menu:\n");
    printf("1. Jouer");
    printf("2. Sauvegarde\n");
    printf("Que voulez-vous faire ?\n");
    printf("Saisir votre choix : ");
    scanf("%d",&choix);

    switch(choix) {
        case 1:
            printf("1. 2 Joueurs");
        printf("2. 4 Joueurs");
        printf("Saisir votre choix : ");
        scanf("%d",&choix2);
        switch(choix2) {
            case 1:
                //Fonction 2 joueurs
                    break;
            case 2:
                //Fonction 4 joueurs
                    break;
        }
        case 2 :
            //Sauvegarde la partie
                break;
    }
}
